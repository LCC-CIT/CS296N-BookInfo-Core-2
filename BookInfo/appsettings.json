{
  "Data": {
    // Seed user--change password after deployment
    "AdminUser": {
      "Name": "Admin",
      "Email": "admin@example.com",
      "Password": "Secret-1",
      "Role": "Admins"
    }
  },
  // These connection strings are only here so they will appear in the Publish settings dialog
  // Be sure to provide the password in the Publish settings dialog
  "ConnectionStrings": {
    // Smarter ASP.NET connection string
    "MsSqlConnection": "Data Source=SQL5004,1433;Initial Catalog=DB_A451DA_BookInfo2;User Id=DB_A451DA_BookInfo2_admin;Password=xxxxxxxx;"
  }
}
// All connection strings that are read by the app are in the development and production appsettings files
// Be sure the ASPNETCORE_ENVIRONMENT environment variable is set to either productionn or development in order to get the right connection string